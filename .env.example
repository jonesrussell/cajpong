# CajPong multiplayer server URL.
# The Flutter client is built with --dart-define=SERVER_URL=... (see deploy.sh).
# For local dev: run the server with npm run dev:server; Flutter default is http://localhost:3000
