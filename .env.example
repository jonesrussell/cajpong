# CajPong multiplayer server URL (client uses this to connect).
# For local dev: run the server with npm run dev:server and leave this unset or set to http://localhost:3000
# For production: set to your deployed server URL (e.g. https://your-app.railway.app)
VITE_SERVER_URL=http://localhost:3000
