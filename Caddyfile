# CajPong on pong.northcloud.one
# TLS is automatic (Let's Encrypt). App runs on localhost:3000.
# Socket.IO WebSocket is handled by reverse_proxy by default.

pong.northcloud.one {
	reverse_proxy 127.0.0.1:3000
}
